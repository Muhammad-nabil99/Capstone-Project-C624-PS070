import Beranda from "../views/pages/beranda"

const routes = {
  '/': Beranda, // default page
  '/beranda': Beranda,
  // '/favourite': Favourite,
  // '/detail/:id': Detail
}


export default routes
